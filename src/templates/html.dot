<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{=it.options.fontName}}</title>
        <link href="css/{{=it.options.fontName}}.css" rel="stylesheet">
        <style>
            body {
                font-size: 1.2rem;
            }
        </style>
    </head>
    <body>
        <h1>{{=it.options.fontName}}</h1>
        {{ for (var glyph of it.glyphs) { }}
            <div>
                <i class="{{=it.prefix}} {{=it.prefix}}-{{=glyph.name}}"></i> <code>.{{=it.prefix}}-{{=glyph.name}}</code>
            </div>
        {{ } }}
    </body>
</html>